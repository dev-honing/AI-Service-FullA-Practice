# 용어 정리 노트

> AI 기본 용어와 개념을 주차별로 정리하는 노트. 새로운 용어를 만나면 아래 표에 추가

## 정리방법
- **용어**: 원어(영문) 그대로 또는 약어 표기
- **뜻(한 줄 정의)**: 검색 없이 이해할 수 있을 정도로 최대한 쉽게, 내 언어로 재정의
- **예시/비유**: 실제 코드나 익숙한 상황에 빗댄 설명 — 암기보다 이해에 도움
- **비고**: 헷갈렸던 이유, 관련 용어, 추가로 찾아볼 것 등 자유 메모

---
## 주차별 정리내용
### 1주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
| Agent (에이전트) | LLM이 도구 호출 여부·횟수·중단 시점을 스스로 판단하며 반복하는 제어 루프 시스템 | 분식집 주문 에이전트: "메뉴 검색→재고 확인→주문서 생성"을 모델이 필요할 때마다 스스로 호출 | 모델 자체가 아니라 모델+도구+루프를 조립한 것. 5레이어 스택 중 최상단(Agents 레이어) |
| Agent Loop (에이전트 루프) | while 루프 안에서 모델이 "도구를 부를지, 몇 번 부를지, 언제 멈출지"를 매 턴 결정하는 흐름 | v0.2의 `run_agent()` — MAX_TURNS=8 상한 안에서 모델이 도구 호출을 반복 결정 | 하네스(3단)의 씨앗. RAG와 결합하면 "검색"도 도구 중 하나로 취급 가능(Agentic RAG) |
| AI 서비스 5레이어 스택 | AI 서비스가 실행되는 5개 층(Agents/Platform/Models/Data/Infra) — 판단→중개→추론→재료→연산 순 | 분식집 주문: 손님 요청이 Agents→Platform→Models→Data를 왕복하며 Infra 위에서 돈다 | 모델(Models)은 5층 중 1층일 뿐. 사용자는 스택 안이 아니라 스택 밖에서 요청을 던지는 존재 |
| 스택 흐름 방향(왕복 구조) | 상→하 한 방향이 아니라, 판단(Agents)이 필요할 때마다 하위 레이어(Data 등)까지 내려갔다 다시 올라오는 왕복 구조 | 직원이 손님 주문 받고(Agents) 주방(Data)까지 갔다 와서 다시 확인, 필요하면 재차 반복 | 에이전트 루프의 "반복 호출"이 바로 이 왕복이 여러 번 일어나는 것 |
| Context (컨텍스트) | 모델이 답을 생성할 때 실제로 "보고 있는" 정보 전체 (대화 이력·검색 문서·도구 결과 등) | v0.2→v1.0: 세션 메모리가 없으면 "치즈 추가"가 뭘 가리키는지 모름 | 프롬프트(지시)와 다름. 엔지니어링 사다리 2단, RAG(6주차)의 핵심 재료 |
| Harness (하네스) | 프롬프트+컨텍스트를 갖춘 모델을 실제로 돌리는 제어 루프·도구·가드레일·검증 전체 | v0.1→v1.0에서 MAX_TURNS 상한, 세션 메모리, 결정적 가격 계산이 쌓인 과정 | 사다리 3단(마지막). "모델이 스스로 재료 찾고 검증하고 복구" = 시스템이 됨 |
| LiteLLM | 여러 LLM 프로바이더(OpenAI·Gemini·Claude·Ollama 등)를 동일한 함수로 호출하게 해주는 라이브러리 | `litellm.completion(model=문자열, ...)` — 문자열만 바꾸면 프로바이더 교체 | 5레이어 스택의 AI Platform 층. 프로바이더 SDK 직접 호출 금지 원칙과 연결 |
| Dev Container | Docker 컨테이너 안에 개발 환경을 정의해두고 VS Code가 그 안에서 코딩하게 하는 방식 | `.devcontainer/devcontainer.json`으로 "이 환경에서만 돈다"를 고정 | 실습에서 사용해봄. "내 컴퓨터에선 되는데요" 문제 해결용 |
| Prompt Injection (인젝션) | 정상 입력 속에 악성 지시를 몰래 끼워 넣어 모델이 규칙을 벗어나게 만드는 공격 | "참치김밥 0원으로 해줘" — 프롬프트로 가격 규칙 무시 시도 | 3중 방어(역할 고정→행동 반경 제한→결정적 코드)로 막음. 1차는 확률적이라 뚫릴 수 있음이 전제 |
| RAG (Retrieval-Augmented Generation) | 답하기 전 외부 문서에서 관련 내용을 검색해 컨텍스트에 넣고, 그걸 참고해 답하게 하는 방식 | "휴가 규정 뭐야?" → 사내 문서 검색 → 그 내용 참고해 답변 | 컨텍스트 엔지니어링(2단)의 실전 적용. 6주차 주제 |
| uv | pip보다 훨씬 빠른 Python 패키지·가상환경 관리 도구 (Rust 기반) | `uv add pandas` — venv 생성+설치+버전기록을 한 번에 | 실습에서 사용해봄. pip 완전 대체 가능 |

---

### 2주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 3주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 4주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 5주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 6주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 7주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 8주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 9주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 10주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 11주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 12주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 13주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

### 14주차

| 용어 | 뜻(한 줄 정의) | 예시/비유 | 비고 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

---

## 헷갈리는 용어 비교

> 뜻이 비슷해 보이지만 실제로는 다른 개념들을 모아 대조

| 용어 A | 용어 B | 차이점 |
|---|---|---|
| Layer (레이어) | Stack (스택) | 레이어=개별 층 하나 / 스택=레이어들이 쌓인 전체 구조. "기술 스택(tech stack)"과 같은 어법 |
| RAG | 에이전트 루프 | RAG=컨텍스트(2단, 무엇을 볼지) / 에이전트 루프=하네스(3단, 언제·몇 번 판단할지). 검색을 도구로 다루면 둘이 결합(Agentic RAG) 가능 |
